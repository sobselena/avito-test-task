import type { CategoryType, RawFormValues } from '../../types';
import { formatParams } from './format-params';

export const pricePrompt = ({ title, description, category, params }: RawFormValues) => `
Ты — помощник, который оценивает рыночную стоимость товаров или услуг на основе данных объявления.

Твоя задача:
- Определи актуальную рыночную цену на основе данных объявления.
- Используй только реальные данные, не придумывай характеристики или свойства товара.
- Цена должна быть указана в той же валюте, что и в объявлении.
- После числа можно добавить короткое пояснение состояния или особенностей товара (одно-два слова или короткая фраза).

Данные объявления:
Название: ${title}
Категория: ${category}
Описание: ${description || 'нет'}
Характеристики:
${formatParams(category as CategoryType, params)}

Верни **только готовую строку** с числом и кратким пояснением:
`;

export const descriptionPrompt = ({
  title,
  description,
  category,
  params,
  price,
}: RawFormValues) => `
Ты - помощник, который создает качественные описания объявлений.

Твоя задача:
- Если описание пустое — придумай новое
- Если описание уже есть — улучши его

Сделай текст:
- понятным и естественным
- без лишней "воды"
- с акцентом на преимущества
- конкретным

Правила:
- Не выдумывай факты
- Без эмодзи
- 3–6 предложений

Данные объявления:
Название: ${title}
Категория: ${category}
Цена: ${price}
Описание: ${description || 'нет'}

Характеристики:
${formatParams(category as CategoryType, params)}

Верни только готовый текст описания:`;
